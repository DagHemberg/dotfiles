return {
  -- 'rcarriga/nvim-notify',
  -- opts = {
  --   render = 'compact',
  --   stages = 'fade',
  -- },
  -- config = function(_, opts)
  --   local notify = require('notify')
  --   vim.notify = notify
  --   notify.setup(opts)
  -- end,
}
